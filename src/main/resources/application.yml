spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://34.244.212.36:3306/emsp?useSSL=false&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
  hibernate:
    dialect:
      storage_engine: innodb
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false

  springfox:
    documentation:
      enabled: true
      title: "eMSP Account Token Service API"
      description: "Spring Boot RESTful API for Account Token Management"
      version: "1.0.0"
      contact:
        name: "Your Name"
        url: "https://your-website.com"
        email: "your-email@example.com"

  server:
    port: 8080
  logging:
    level:
      root: DEBUG
      com.baomidou.mybatisplus: debug
      org.mybatis: debug
      com.zaxxer.hikari: debug
      org.springframework.jdbc: debug
#logging:
#  level:
#    root: info





#mybatis-plus:
#  configuration:
#    map-underscore-to-camel-case: true
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#  type-handlers-package: com.yzy.config
#  map-underscore-to-camel-case: true
#  default-enum-type-handler: com.baomidou.mybatisplus.core.handlers.MybatisEnumTypeHandler
#  global-config:
#    db-config:
#      id-type: auto
#      logic-delete-value: 1
#      logic-not-delete-value: 0
#  mapper-locations: classpath:mappers/*.xml
请继续维护此仓库。开始前完整阅读 README.md、
docs/current-system-guide.md、docs/implementation-plan.md、
docs/architecture-decisions.md、docs/public-test-corpus.md。

这是一个 KCI & GRAS Confluence Page Intelligence Agent。
工作树可能包含未提交修改；必须保留，禁止 git reset --hard、
git checkout -- 或覆盖无关文件。

当前已完成：增量采集、Page IR、Azure OpenAI 受约束页面理解、
Review Queue、附件重析、确定性 Extraction Plan 执行、
metric_observations/current_actions MongoDB 持久化。
下一优先项：真实内网 Confluence/Azure shadow run、人工附件重析后自动恢复事实执行、
变化事件和日报。


RESILIENCE_MONGODB_URI
RESILIENCE_MONGODB_DATABASE
AZURE_OPENAI_ENDPOINT
AZURE_OPENAI_API_KEY
AZURE_OPENAI_DEPLOYMENT
CONFLUENCE_BEARER_TOKEN
OPENAI_COMPATIBLE_BASE_URL
